import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginHorizontal: '10%',
    marginBottom: 20,
  },
  textStyle: {
    fontSize: 24,
    fontWeight: '400',
  },
});

export default styles;
